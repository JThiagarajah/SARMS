import { ReadOnlyResults } from "../shared/ReadOnlyResults";

export function DeanOfferingDetail() {
  return <ReadOnlyResults headerNote="Read-only view. The Dean has no edit, approve, or release action anywhere in SARMS." />;
}
