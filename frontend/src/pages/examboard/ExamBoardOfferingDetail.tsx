import { ReadOnlyResults } from "../shared/ReadOnlyResults";

export function ExamBoardOfferingDetail() {
  return <ReadOnlyResults headerNote="Officially released results only — the Examination Branch never sees a result before release." />;
}
